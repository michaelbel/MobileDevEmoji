# Agent Instructions

- Use `./gradlew` for builds and tests to ensure consistent tooling.
- When adding or changing content, keep documentation in English unless the context requires otherwise.
- Prefer `rg` for searching the codebase instead of recursive `grep` or `ls -R`.
- Before committing, format Kotlin code with `./gradlew ktlintFormat` if you modify Kotlin sources.
